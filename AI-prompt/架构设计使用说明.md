在提供的maximo应用架构字段信息中,必填项用(*),只读项用(-),前端隐藏用(/),不标识的为前端可填可不填正常文本框
如果组件有对应的必填样式,只读样式,则使用对应的组件样式
隐藏的属性前端就不要显示就行了

创建人,创建时间,状态首选位置放到右边,创建时间精确到秒

因为这是做演示用的页面,保留测试数据方便演示

只能使用项目根目录下的 maximo-ui/maximo-style.html 中的组件
