server.port: 21001

solon.app:
  name: "maximo-db2-mcp"
  group: "maximo"

#solon.include: app-db.yml
# 引入数据库配置(敏感信息独立管理)
solon.config.load:
  - "app-db.yml"
#  - "app-db-example.yml"


# ===================================================
# MCP 服务配置
# ===================================================
server.http.maxBodySize: 52428800

# 日志级别
solon.logging:
  level:
    root: "info"
    com.maximo: "debug"
